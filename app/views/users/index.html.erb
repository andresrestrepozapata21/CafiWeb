<%= form_with url: search_path, local: true, method: :get do |form| %>
  <strong>Filtrar</strong>
  <%= form.text_field :q, placeholder: "Correo", data: {behavior: "autocomplete"}%>
<% end %>
<div class="row align-middle title_usuarios">
  <div class="columns text-right large-5" style="padding-bottom: 10px; color: green;">
    <i class="fa fa-users fa-3x"></i>
  </div>
  <div class="columns large-4 titulo_usuarios"> 
    <h2>Usuarios</h2> 
  </div>
</div>
<div class="row">
  <div class="columns large-12">
    <table>
      <thead>
        <tr>
          <th>Documento</th>
          <th>Nombre</th>
          <th>Telefono</th>
          <th>Dirección</th>
          <th>Email</th>
          <th>Monto</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
      <% if @one %>
        <% @users.each do |user| %>
          <tr>
            <td><%= user.document %></td>
            <td><%= user.name %></td>
            <td><%= user.phone %></td>
            <td><%= user.address %></td>
            <td><%= user.email %></td>
            <td><%= user.money_amount %></td>
            <td><%= link_to 'Editar', edit_user_path(user), :class =>"clear button success" %></td>
            <td><%= link_to 'Eliminar', user, method: :delete, data: { confirm: 'Estas Seguro?' }, :class =>"clear button alert" %></td>
          </tr>
        <% end %>
      <% else %>
        <tr>
          <td><%= @users.document %></td>
          <td><%= @users.name %></td>
          <td><%= @users.phone %></td>
          <td><%= @users.address %></td>
          <td><%= @users.email %></td>
          <td><%= @users.money_amount %></td>
          <td><%= link_to 'Editar', edit_user_path(@users), :class =>"clear button success" %></td>
          <td><%= link_to 'Eliminar', @users, method: :delete, data: { confirm: 'Estas Seguro?' }, :class =>"clear button alert" %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>

<br>
